name = "parsa-portfolio"
compatibility_date = "2024-01-01"

# Cloudflare Pages project
# Deploy with: npx wrangler pages deploy dist --project-name=parsa-portfolio

[env.production]
# Production environment variables (set sensitive ones in CF dashboard)

# pages_build_output_dir is used when deploying via `wrangler pages deploy`
# Build command: npm run build
# Build output: dist
